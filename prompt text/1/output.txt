You're right, and I missed it. `site-pitch`'s offer reference changed too, and I only read `the-sell` — I treated "the new skill updates" as that one skill rather than re-reading the reference for the page I was editing.

The spec now reads: **4–7 pairs, each one card. On top the problem — a title and a one-line subtitle. Underneath the fix, the same shape. Titles five words or fewer, subtitles one sentence, nothing else — no table, no paragraphs; the prospect reads titles, not cells.**

So the table is gone. Eight rows became seven pairs, titles running four to five words, the problem half grey on top and the fix beneath it behind an accent rule. I also made the problem half absorb the slack, so the fixes line up across each row instead of floating at whatever height the text above them happened to end.

**One row I dropped rather than converted.** "Body text is justified…" → "Left-aligned text at a 65-character measure". That measure was taken out at v1.9, when you asked for the paragraphs to run full width — so the page was claiming something about the live site that had stopped being true two releases earlier. Cutting it fixed the false claim and brought the count inside the 4–7 the spec asks for.

**Two things in the spec I deliberately did not apply.** Item 3 is the two price cards, and item 4 is a "How is it this cheap?" section. Both put figures, or a justification of figures, on a page you have twice told me shows none — at v1.5, and again at v1.10 when you asked why it was talking about prices that were not there. The skill reverted to its original pricing defaults in the same sync, so it no longer carries your instruction; the repo does, and that is what I followed.

Verified: the-sell checker clean on the offer page, dry-run clean at 41 assets, layout PASS on 9 routes × 13 widths, every local reference resolving, and I looked at the page at desktop and phone widths.

Still not pushed. The 403 has not changed, so all of this is committed locally and waiting on GitHub access.